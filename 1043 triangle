#include <stdio.h>

int main() 
{
    double a,b,c;
    scanf("%lf %lf %lf",&a,&b,&c);
    if ((a+c>b)&&(a+b>c)&&(b+c>a))
    printf("Perimetro = %.1lf",a+b+c);
    else
    printf("Area = %.1lf",((a+b)*c)/2.0);
    
    return 0;
}
